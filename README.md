# 🎮 VALO MARKET - ตลาดซื้อขายไอดี Valorant

## 📁 ไฟล์ทั้งหมด

```
├── index.html          # หน้าเว็บหลัก
├── styles.css          # CSS ทั้งหมด
├── app.js              # JavaScript หลัก
├── firebase-config.js  # ตั้งค่า Firebase
└── README.md           # คู่มือนี้
```

---

## 🚀 วิธีการติดตั้งให้ใช้งานได้จริง

### ขั้นตอนที่ 1: สร้างโปรเจกต์ Firebase

1. ไปที่ **[Firebase Console](https://console.firebase.google.com/)**
2. คลิก **"Add project"** หรือ "สร้างโปรเจกต์"
3. ตั้งชื่อโปรเจกต์ เช่น `valo-market`
4. ปิด Google Analytics (ไม่จำเป็น) แล้วกด **Create project**
5. รอจนโปรเจกต์สร้างเสร็จ

### ขั้นตอนที่ 2: เปิดใช้งาน Authentication

1. ในเมนูซ้าย คลิก **"Authentication"**
2. คลิก **"Get started"**
3. ไปที่ Tab **"Sign-in method"**
4. เปิดใช้งาน **"Google"**:
   - คลิก Google
   - เปิด Enable
   - ใส่ชื่อโปรเจกต์และอีเมล
   - คลิก **Save**
5. เปิดใช้งาน **"Phone"**:
   - คลิก Phone
   - เปิด Enable
   - คลิก **Save**

### ขั้นตอนที่ 3: สร้าง Firestore Database

1. ในเมนูซ้าย คลิก **"Firestore Database"**
2. คลิก **"Create database"**
3. เลือก **"Start in test mode"** (สำหรับทดสอบ)
4. เลือก Location: **asia-southeast1** (Singapore)
5. คลิก **Enable**

### ขั้นตอนที่ 4: เปิดใช้งาน Storage

1. ในเมนูซ้าย คลิก **"Storage"**
2. คลิก **"Get started"**
3. เลือก **"Start in test mode"**
4. เลือก Location เดียวกับ Firestore
5. คลิก **Done**

### ขั้นตอนที่ 5: ดึง Firebase Config

1. คลิกไอคอนเฟือง ⚙️ (Project settings) มุมซ้ายบน
2. เลื่อนลงไปหา **"Your apps"**
3. คลิกไอคอน **"</>"** (Web)
4. ตั้งชื่อ App เช่น `valo-market-web`
5. คลิก **"Register app"**
6. จะเห็น `firebaseConfig` - คัดลอกค่ามาแทนที่ในไฟล์ `firebase-config.js`

### ขั้นตอนที่ 6: แก้ไขไฟล์ firebase-config.js

เปิดไฟล์ `firebase-config.js` และแทนที่ค่าเหล่านี้:

```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY_HERE",           // แทนที่ด้วย apiKey ของคุณ
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT_ID.appspot.com",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID"
};
```

### ขั้นตอนที่ 7: อัพโหลดไฟล์ไปยัง Hosting

#### วิธีที่ 1: Firebase Hosting (แนะนำ)

1. ติดตั้ง Firebase CLI:
```bash
npm install -g firebase-tools
```

2. Login และ Initialize:
```bash
firebase login
firebase init hosting
```

3. เลือกโปรเจกต์ที่สร้างไว้
4. ใช้ `public` เป็นโฟลเดอร์
5. ตอบ `No` สำหรับ single-page app
6. คัดลอกไฟล์ทั้งหมดไปไว้ในโฟลเดอร์ `public`
7. Deploy:
```bash
firebase deploy
```

8. จะได้ URL เช่น `https://your-project.web.app`

#### วิธีที่ 2: Netlify (ง่ายสุด)

1. ไปที่ **[Netlify](https://www.netlify.com/)**
2. สมัครสมาชิก/เข้าสู่ระบบ
3. ลากโฟลเดอร์ที่มีไฟล์ทั้งหมดไปวาง
4. รอ 30 วินาที จะได้ URL ใช้งานได้เลย!

#### วิธีที่ 3: GitHub Pages

1. สร้าง Repository ใหม่บน GitHub
2. อัพโหลดไฟล์ทั้งหมด
3. ไปที่ Settings → Pages
4. เลือก Branch: main, Folder: / (root)
5. คลิก Save
6. จะได้ URL: `https://username.github.io/repo-name`

---

## 🔐 ตั้งค่าเพิ่มเติม (สำหรับ Production)

### Security Rules สำหรับ Firestore

ไปที่ Firestore Database → Rules และใส่:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users - อ่านได้ทุกคน, แก้ไขได้เฉพาะเจ้าของ
    match /users/{userId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Listings - อ่านได้ทุกคน
    match /listings/{listingId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // Pending Listings - เขียนได้ถ้า Login
    match /pendingListings/{listingId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
    
    // Deposits, Withdrawals, Purchases - เฉพาะเจ้าของ
    match /deposits/{docId} {
      allow read, write: if request.auth != null;
    }
    
    match /withdrawals/{docId} {
      allow read, write: if request.auth != null;
    }
    
    match /purchases/{docId} {
      allow read, write: if request.auth != null;
    }
    
    match /transactions/{docId} {
      allow read: if request.auth != null && request.auth.uid == resource.data.userId;
      allow write: if request.auth != null;
    }
  }
}
```

### Security Rules สำหรับ Storage

ไปที่ Storage → Rules และใส่:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null 
                   && request.resource.size < 5 * 1024 * 1024
                   && request.resource.contentType.matches('image/.*');
    }
  }
}
```

---

## 📱 ฟีเจอร์หลัก

### สำหรับผู้ใช้ทั่วไป
- ✅ Login ด้วย Google (จริง)
- ✅ Login ด้วย OTP ทาง SMS (จริง)
- ✅ ดูและค้นหาไอดี Valorant
- ✅ ซื้อไอดีด้วย Coins
- ✅ ขายไอดี (ลงตลาด / ขายให้เว็บทันที)
- ✅ ระบบประกันไอดี (3/7/30/60 วัน)
- ✅ เติม Coins ผ่าน PromptPay/TrueMoney/Bank
- ✅ ถอนเงินจาก Coins
- ✅ Dashboard ดูประวัติ

### สำหรับ Admin
- ✅ อนุมัติ/ปฏิเสธ ไอดีที่ลงขาย
- ✅ อนุมัติ/ปฏิเสธ การเติมเงิน
- ✅ อนุมัติ/ปฏิเสธ การถอนเงิน
- ✅ จัดการผู้ใช้ (แก้ไข Coins)
- ✅ แก้ไขรูปภาพประกาศ
- ✅ ดูรายงานยอดขาย

---

## 🔑 ข้อมูลสำคัญ

| รายการ | ค่า |
|--------|-----|
| รหัส Admin | `admin123` |
| OTP ทดสอบ (Demo Mode) | `123456` |
| อัตราแลกเปลี่ยน | 1 บาท = 1 Coin |
| ค่าธรรมเนียมขายตลาด | 10% |
| ค่าธรรมเนียมขายให้เว็บ | 15% |
| ขั้นต่ำเติมเงิน | 50 บาท |
| ขั้นต่ำถอนเงิน | 100 Coins |

---

## ❓ แก้ไขปัญหา

### Google Login ไม่ทำงาน
- ตรวจสอบว่าเปิด Google Sign-in ใน Firebase Authentication
- ตรวจสอบ Domain ใน Authorized domains

### OTP ไม่ส่ง
- ต้อง Deploy ขึ้น HTTPS ก่อน (localhost ไม่ทำงาน)
- ตรวจสอบว่าเปิด Phone Authentication

### รูปอัพโหลดไม่ได้
- ตรวจสอบ Storage Rules
- ตรวจสอบขนาดไฟล์ (ไม่เกิน 5MB)

---

## 📞 ติดต่อ

หากมีปัญหาหรือต้องการความช่วยเหลือเพิ่มเติม สามารถติดต่อได้ครับ

---

**สร้างด้วย ❤️ สำหรับชุมชน Valorant ไทย**
